# Everything the image does not need. Keeps the build context small and makes it
# impossible to bake a local .env or state file into a layer.
.env
.env.*
!.env.example
state.json
*.state.json

.git
.github
.gitignore

node_modules
npm-debug.log*

README.md
Dockerfile
.dockerignore
.keepalive
